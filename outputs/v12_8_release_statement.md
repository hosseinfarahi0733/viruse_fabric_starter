# VF-H2 v12.8.0 — Release Statement

Use the following compact project ledger entry:

```text
v12.7.0 build-failing tag; non-release-bearing
v12.7.1 build-failing tag; non-release-bearing
v12.7.2 clean build generalized transport certificate
v12.8.0 release-correction audit and manifest
```

The theorem-bearing generalized certificate milestone is `v12.7.2`, not
`v12.7.0` or `v12.7.1`.

The audit-bearing milestone is `v12.8.0`.
